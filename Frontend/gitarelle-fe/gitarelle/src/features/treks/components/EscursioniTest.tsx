const EscursioniTest = () => {

  // Array di 3:
  const array = [
    1, 2, 3
  ]

  // Uso map per stamparle in console
  array.map(numero => console.log(numero));

  return (
    <div>
    </div>
  )
}

export default EscursioniTest
